#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }


    /// Converts a number to a string representating roman numeral.
    fn num_as_roman(num: i32) -> String {
        ...
    }
}
